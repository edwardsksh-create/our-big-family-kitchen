# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://glyukuiofsurjwlluhqe.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_BuWIZw3oUCtWhjO8RsV-aQ_VrOEPepK
SUPABASE_SERVICE_ROLE_KEY=
# Shared JWT secret for the NextAuth → Supabase bridge (mobile/native clients).
# Supabase dashboard → Settings → API → JWT Secret. Used by /api/v1/auth/token.
SUPABASE_JWT_SECRET=

# Auth
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=

# Resend (transactional email + magic link)
RESEND_API_KEY=
EMAIL_FROM="Our Big Family Kitchen <hello@ourbigfamilykitchen.com>"

# Admin
ADMIN_EMAIL=

# AI (Anthropic Claude API — used by /add/paste and /add/url fallback)
ANTHROPIC_API_KEY=

# Cron auth — secret used by Vercel cron jobs to call /api/cron/*
# Generate with: openssl rand -base64 32
CRON_SECRET=
